# Example environment configuration for local development
# Copy this file to `.env` or `.env.local` and fill in values as needed.
# Do NOT commit real secrets.

# Custom environment name (use for staging/preview, etc.).
# Allowed values are up to you, e.g. development | staging | preview | production
APP_ENV=development
NODE_ENV=$APP_ENV

# Exposed to the browser (safe values only)
NEXT_PUBLIC_APP_ENV=$APP_ENV
NEXT_PUBLIC_SITE_URL=http://localhost:8080
# Vercel sets NEXT_PUBLIC_VERCEL_ENV automatically in deployments
NEXT_PUBLIC_VERCEL_ENV=$APP_ENV


# Notes
# - In Next.js, environment variable load order is:
#   1) process.env
#   2) .env.$(NODE_ENV).local (ignored during test)
#   3) .env.local (ignored during test)
#   4) .env.$(NODE_ENV)
#   5) .env
# - For client-side usage, variables MUST be prefixed with NEXT_PUBLIC_.
# - On Vercel, set variables in Project Settings; they will be injected automatically.
# - For local dev, `vercel env pull` will populate a local `.env` file (ignored by git).


# Optional: set locally; Vercel sets this in deployments

# Created by Vercel CLI
NEXT_PUBLIC_SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
NEXT_PUBLIC_SUPABASE_URL="https://nlxdrbqstjodlkrsisbd.supabase.co"
POSTGRES_DATABASE="postgres"
POSTGRES_HOST="db.nlxdrbqstjodlkrsisbd.supabase.co"
POSTGRES_PASSWORD=""
POSTGRES_PRISMA_URL=""
POSTGRES_URL=""
POSTGRES_URL_NON_POOLING=""
POSTGRES_USER="postgres"
SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5seGRyYnFzdGpvZGxrcnNpc2JkIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTMwOTU4MzcsImV4cCI6MjA2ODY3MTgzN30.BRODsGG0ENL3vnEzWcP5_a-_-60FyJxkzZVVTdgDK2k"
SUPABASE_JWT_SECRET=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_URL="https://nlxdrbqstjodlkrsisbd.supabase.co"